@model List<Usuario>
<h1>Usuários Cadastrados no Sistema</h1>

<p class="text-danger">@ViewData["Mensagem"]</p>

<table class="table">
    <thead>
        <tr>
            <th scope="row">Nome</th>
            <th scope="row">Login</th>
            <th scope="row">Tipo</th>
        </tr>
    </thead>

    <tbody>
        @foreach (Usuario u in Model)
        {
            <tr>
                <td>@u.Nome</td>
                <td>@u.login</td>
                @if(u.tipo==Usuario.ADMIN)
                {
                    <td>Administrador</td>
                }
                else
                {
                    <td>Padrão</td>
                }
                <td><a href="/Usuarios/EditarUsuario?Id=@u.Id">Editar</a></td>
                <td><a href="/Usuarios/ExcluirUsuario?Id=@u.Id">Excluir</a></td>
            </tr>
        }
    </tbody>
</table>

<a href="RegistrarUsuarios">Registrar novo Usuário</a>